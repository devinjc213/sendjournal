import React from 'react'
import Form from './FormContainer.js'

function ClimbContainer(props){
  if(props.currentStep !== 4) {
    return null
  }

  return (
    <div>
      what the fuck
    </div>
  )
}

export default ClimbContainer
